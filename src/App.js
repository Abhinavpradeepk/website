import React from "react";
import Home from "./home"; // Adjust the path if the file is in a different folder

function App() {
    return (
        <div>
            <Home />
        </div>
    );
}

export default App;
